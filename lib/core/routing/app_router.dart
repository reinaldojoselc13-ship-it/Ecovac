import 'package:flutter/widgets.dart';

class AppRouter {
  static Route<dynamic>? onGenerateRoute(RouteSettings settings) {
    // Placeholder: define rutas nominales de la app
    return null;
  }
}
