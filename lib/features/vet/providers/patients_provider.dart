import 'package:flutter/foundation.dart';

class PatientsProvider extends ChangeNotifier {
  // Provider purgado: las operaciones sobre pacientes han sido removidas.
  // Se mantiene la clase como stub para evitar fallos en imports existentes.
}
